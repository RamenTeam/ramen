query getUser($data: GetUserDto!){
    getUser(data: $data){
        id
        email
        username
        isVerified
        password
        firstName
        lastName
        status
        name
    }
}